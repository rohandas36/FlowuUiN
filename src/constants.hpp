#ifndef CONST_HPP
#define CONST_HPP

extern int 	GRID_X,
			GRID_Y,
			GRID_Z,
			QUALITY,
			NUM_PARTICLE,
			FRAME_WINDOW
			;

extern float THRESHOLD,
			CUTOFF_RADIUS,
			COLL_SCAN_RADIUS,
			REND_SCAN_RADIUS,
			SIDE_LENGTH,
			DEL_T
			;
			
#endif

